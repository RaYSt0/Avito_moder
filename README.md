## Система модерации объявлений Авито

Полноценное решение тестового задания: фронтенд-панель модератора (`Frontend/`) + готовый API (`Backend/`).

### Стек
- Vite + React 18 + TypeScript
- Ant Design 5 в качестве UI-библиотеки
- React Router 6
- @tanstack/react-query для работы с API и отмены запросов
- Axios, Recharts, react-hotkeys-hook

### Возможности
- 📋 Лента объявлений `/list`
  - карточки с ценой, статусом, категорией, приоритетом, датой
  - фильтрация: статус (множественный), категория, диапазон цен, поиск, сохранённые пресеты
  - сортировки по дате, цене, приоритету + пагинация (10 элементов)
  - сброс фильтров, отображение общего количества, горячая клавиша `/` для быстрого поиска
- 🔍 Детальная страница `/item/:id`
  - галерея фото, описание, характеристики, данные продавца
  - история модерации, панель действий (одобрить, отклонить, вернуть на доработку)
  - обязательный ввод причины при отклонении/доработке, быстрые шаблоны, горячие клавиши `A/D/←/→`
  - навигация «предыдущее/следующее», кнопка «назад к списку»
- 📊 Статистика `/stats`
  - метрики (total, % решений, среднее время)
  - график активности за выбранный период (сегодня/7/30 дней)
  - круговая диаграмма решений, распределение по категориям
  - экспорт данных в CSV
- 🌗 Тёмная тема (сохранение в localStorage)
- 🔁 Отмена запросов при смене фильтров/страниц (через AbortController + React Query)

### Установка и запуск
Требования: Node.js 20+, npm 10+.

```bash
# 1. API
cd Backend
npm install
npm run dev          # сервер на http://localhost:3001

# 2. Фронтенд
cd ../Frontend
npm install
npm run dev          # приложение на http://localhost:5173
```

> При необходимости задайте кастомный адрес API:
> ```
> # Frontend/.env.local
> VITE_API_BASE_URL=http://localhost:3001/api/v1
> ```

### Структура
- `Frontend/src/api` – обёртки над REST-эндпоинтами
- `Frontend/src/hooks` – работа с фильтрами, пресетами, модератором
- `Frontend/src/pages` – страницы `/list`, `/item/:id`, `/stats`
- `Frontend/src/providers` – общие провайдеры (темы, router, React Query)
- `Frontend/src/utils` – форматирование дат/цен, навигация между объявлениями
- `Backend/src` – контроллеры, роуты и мок-данные API

### Тесты и дальнейшее развитие
- Добавить unit-тесты (React Testing Library + Vitest)
- Реализовать bulk-операции и PDF-отчёты из списка «звёздочек»
- Расширить real-time обновления через WebSocket или SSE

### Почему такие решения
- **React Query**: кэширование, рефетч, отмена запросов «из коробки» — критично при сложных фильтрах.
- **Ant Design**: ускоряет сборку интерфейса с богатыми компонентами (таблицы, графика, формы).
- **Recharts**: минимум кода для реализации требуемых графиков.
- **Сохранённые фильтры + тёмная тема + хоткеи** — самые полезные из «звёздных» пунктов с адекватной трудоёмкостью.

